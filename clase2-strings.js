var nombre = 'Juan', apellido = 'Pereyra'

var nombreMayus = nombre.toUpperCase()

var apellidoMinus = apellido.toLocaleLowerCase()

var primeraLetraNombre = nombre.charAt(0)

var cantidadLetrasNombre = nombre.length

var nombreCompleto = `${nombre} ${apellido.toUpperCase()}` //Interpolar variables

var str = nombre.substr(1,2)